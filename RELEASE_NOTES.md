# Release Notes

## v1.2.2

**修复同步协议长连接被中断的问题，并让网络错误提示可读。**

- **网络错误提示中文化**：SSL 握手失败、连接超时、域名解析失败、连接被拒绝、对端中断等底层错误，现在会翻译成用户能看懂、并能据此行动的提示。例如 SSL 中断会提示「可能是系统代理/VPN 未正确转发该域名，可关闭代理直连重试，或稍后再试」，同时保留原始信息便于排查。
- **「自动」协议模式支持网络中断回退**：同步协议是一次请求挂到出图的长连接（可能 1~3 分钟），容易被中间网络设备掐断或服务方提前断开。现在「自动」模式遇到这类网络中断会自动改用异步媒体协议重试。鉴权失败、参数错误、域名填错等重试无意义的情形不会回退，避免无谓的重复请求。
- **实践建议**：若某模型的同步接口频繁中断，直接把该模型条目的协议设为「异步媒体」最稳妥（短请求 + 轮询）。
- 单元测试扩充到 69 项（新增协议回退判据与网络错误归一化用例）。
- 前端入口升级到 `ui/index.1.2.2.js`。

## v1.2.1

**修复：数据目录迁移到插件目录之外，升级/卸载插件不再丢失图库与配置。**

- **数据目录迁出插件目录**：从 `plugins/qwenpaw-image-gen/data/` 迁到 `%APPDATA%\QwenPaw\qwenpaw-image-gen\`。此前卸载或重装插件会连带删除插件目录，导致图库记录、分类、星级备注、生成结果与 API 配置一并丢失（已实测确认会发生）。
- **首次启动自动迁移旧数据**：只复制、不删除旧数据，且幂等（新位置已有同名文件则跳过），重复启动不会覆盖新数据。
- **新增每日自动备份**：每天首次启动备份一次数据库，保留最近 7 份，意外损坏时可回退。
- **API 生图任务字典加上限**：只保留最近 40 个任务，避免长期运行内存增长（与原有生图任务一致）。
- **参考图上传前自动压缩**：最长边压到 2048、JPEG 88%，避免请求体过大被服务方拒绝或上传过慢；压缩失败自动回退原始字节。
- **缓存自动清理**：参考图与上传队列缓存超过 7 天自动清理，绝不动图库正式图片。
- **同步协议取消语义更准确**：按钮由「取消」改为「停止等待」并加说明——同步请求无法中途撤回，服务方可能已出图并计费；异步协议仍可真正取消任务。
- 前端入口升级到 `ui/index.1.2.1.js`；新增数据目录迁移与备份测试（18 项）。

## v1.2.0

**新增「API生图」标签页：可直接调用第三方图像接口出图，不依赖本机显卡与 ComfyUI。**

- **多服务管理**：可添加任意数量的服务端点（服务地址 + API Key），支持启用/停用、编辑、删除、一键测试连接。地址与密钥只保存在本机插件数据库。
- **多模型条目**：每个模型条目绑定一个服务，填写服务方文档里的 model 值即可；支持自定义显示名、协议、能力标签与启用状态。
- **两套协议 + 自动回退**：
  - OpenAI 兼容同步协议（`/images/generations` 文生图、`/images/edits` 图生图 multipart 上传本地参考图）；
  - 异步媒体协议（`/media/generate` 创建任务 + `/media/status` 轮询，`is_final` 后取 `result_url`）；
  - 协议选「自动」时先试同步，若服务方明确提示该模型不支持此端点则自动改用异步。
- **参数模板（通用，不绑定厂商）**：内置 6 类参数形态模板（基础文生图 / 文生图+质量档 / 比例+分辨率 / 透明底 / 多参考图 / OpenAI 图生图编辑）。面板按模型条目的参数结构自动渲染下拉或数字输入框。
- **参考图**：支持从图库多选（可连点多张）与本地上传，自动传给服务方。
- **结果直接进图库**：与 ComfyUI 生成的图片共用同一个图库，分类、星级、备注、放大、批量管理、产物库联动全部照常。
- **进度与取消**：异步任务实时显示状态（排队/运行/进度/完成），可随时取消。
- **错误可读化**：把服务方原始错误整理成中文提示（鉴权失败 / 无权限 / 地址不存在 / 频率或额度 / 参数被拒 / 服务暂不可用），并保留服务方原文便于排查。
- 前端入口升级到 `ui/index.1.2.0.js`；新增 `backend/api_image_adapter.py`（自带 60 项离线单元测试）。

## v1.1.10

- **分块放大优先用 MultiDiffusion（社区进阶方案 2）**：安装了 `ComfyUI-TiledDiffusion` 节点；分块模式在目标 ≤400 万像素时自动改用 `TiledDiffusion`（method=MultiDiffusion，全局 latent 融合），从根本上避免「每块独立生成」导致的长新主体；超大图仍退回 UltimateSDUpscale（后者内部对 VAE 分块，更适合超大分辨率）。分块大小默认 768、重叠 64、批 4。
- **Tile ControlNet 自动启用（社区进阶方案 3）**：下载了 SDXL `xinsir_controlnet_union_sdxl_promax`（2.3GB）；当 denoise ≥ 0.3 时自动用 `ControlNetApply`（强度 0.9）把参考图作为 tile 约束，抑制分块幻觉和接缝。无该模型/节点时自动跳过，完全向后兼容。
- **补充放大模型**：新增 `4x-UltraSharp.pth`（写实/通用，色彩中性），会在放大模型下拉里自动出现。
- 实测：832×1216（带 LoRA）×1.5 分块（MultiDiffusion + Tile ControlNet）→ 1248×1824，LoRA 完整保留，耗时约 9 分钟（8G 显存跑 SDXL + 2.3G CN 需走 CPU offload，慢但可用；追求速度可把 denoise 调到 0.25 以下自动跳过 CN）。
- 前端入口升级到 `ui/index.1.1.10.js`。

## v1.1.9

- **新增「迭代放大」模式**（社区进阶方案 1）：基于 Impact Pack 的 `IterativeImageUpscale` + `DenoiseScheduleHookProvider` + `PixelKSampleUpscalerProvider`，把 2x→4x 拆成多步渐进补细节，denoise 逐步下降，避免一次性大改出伪影。可调「迭代步数」，默认 2 步。
- **新增「脸部细化」开关**（社区进阶方案 1）：放大流程末尾接 `FaceDetailer`（UltralyticsDetectorProvider + face_yolov8m），低幅度再修一次脸，人像/立绘建议开启。角色 LoRA 会正确接入 FaceDetailer 的模型输入，避免修脸后人物变样。
- **环境补齐**：本机 ComfyUI 的 Impact-Pack 子包（ComfyUI-Impact-Subpack）此前不完整，导致 `UltralyticsDetectorProvider` 缺失；已补装该子包（模型 face_yolov8m.pt 本就在）。
- 实测：1104×688（带 lacrimosa LoRA）×1.5 迭代放大 + 脸部细化 → 1656×1032，LoRA 与触发词完整保留，耗时约 6.5 分钟（迭代模式比单次放大慢，适合追求质量）。
- 前端入口升级到 `ui/index.1.1.9.js`。

## v1.1.8

- **ComfyUI 卡死时快速失败并明确提示**：之前 ComfyUI 进程卡死（HTTP 无响应）时，插件会闷头重试到 300 秒超时，用户只看到「一直 0%」。现在连续 5 次连接失败（约 1 分钟）就直接失败并提示「ComfyUI 无响应（进程可能已卡死或退出），请重启 ComfyUI 后重试」。
- **面板实时步进进度验证通过**：面板直连 ComfyUI websocket，能实时看到 ESRGAN 放大阶段的进度（如 1/6）和每个分块的采样步数（如 5/20 步），不会再全程 0%。
- 排查记录：一次「分块卡 0%」的现场是 ComfyUI 进程本身卡死（此前对正在跑分块采样的 ComfyUI 发过 `/interrupt`，UltimateSDUpscale 被中断后模型状态不干净），重启 ComfyUI 后同参数重跑成功（1104×688 ×1.5 → 1656×1032，约 5 分钟，LoRA 与触发词完整保留）。
- 前端入口升级到 `ui/index.1.1.8.js`。

## v1.1.7

- **修复放大后人物形象漂移（LoRA 触发词被画质词顶掉）**：排查确认 LoRA 一直有加载（ComfyUI 历史里能看到 LoraLoader 节点），真正的原因是「自动」提示词策略把提示词整个换成了纯画质词——角色 LoRA 的触发词（如 `lacrimosa`）和角色描述全没了，低 denoise 下人物脸型/发色也会漂。
  - 「自动」策略改为按 denoise 分段：**denoise ≤ 0.25 沿用原图提示词**（触发词还在，人物不会变）；**denoise > 0.25 用画质词**（防止空背景块长出多余内容）。
  - 显式选择「只用画质词 / 沿用原图提示词」不受影响。
  - 面板选项和说明文案同步更新。
- **面板文案去个性化（面向社区发布）**：把面板里写死的助手昵称统一改成「AI 助手」，并扫描了全部面板/后端静态文案，确认没有其他个人路径、昵称或本机信息。
- 前端入口升级到 `ui/index.1.1.7.js`。

## v1.1.6

- **修复「高清修复（hires）在大图上永远 0%」**：hires 会把整张图一次性送进 KSampler，你那次是 3072×1728 放大 1.5× → **4608×2592（11.9M 像素）**，8G 显存直接吃满（7.7/8.2G），SDXL 在这种分辨率下每步要几十秒甚至更久，跑了 19 分钟还没出结果。
  - 新增安全阈值 `HIRES_SAFE_PIXELS = 3_000_000`：hires 的目标分辨率超过它时**自动改用分块高清修复**，并在任务消息里说明「目标 1664×2432（4.0M 像素）超出整图高清修复的安全范围，已自动改用分块高清修复」。
  - 放大参数区文案补充了这个限制。
- **新增 ComfyUI 实时步进进度**：面板直连 ComfyUI 的 websocket，单张任务显示「采样进度 5 / 15 步（33%）」，进度条按步数走，不再全程 0%（原进度条只统计「已完成张数」，单张任务永远显示 0%）。
- 前端入口升级到 `ui/index.1.1.6.js`。

## v1.1.5

- **修复「放大完成后弹窗不显示图片」**：结果弹窗和详情预览原来都是「在当前图库已加载的那一页里按 id 找图」（`(imgs[0]||[]).find(...)`），一旦放大结果保存到别的分类（放大页的「保存分类」与图库当前筛选分类不一致），或者刚好撞上 `openReview` 先 `loadImages()` 清空列表的竞态，就会整块空白。
  - 新增 `GET /images/batch?ids=1,2,3`：按 id 批量取图，与图库分页/分类筛选无关。
  - 前端新增图片缓存 `imageCache` + `findImage/ensureImage`：弹窗按 id 直接取图，取不到就自动拉 `GET /images/{id}`，期间显示「加载中…」而不是空白。
  - `openReview` 拿到结果后，如果结果都在同一个分类且与当前筛选不同，自动把图库切到该分类，避免「弹窗有图、图库里却看不到」。
- 前端入口升级到 `ui/index.1.1.5.js`。

## v1.1.4

- **修复「✨ 让 AI 出图」在 img2img 模式下指令不带参考图**：`aiOneShot` 生成的指令文本原来只拼了想法 / 模型 / LoRA，所以 AI 拿到的指令和文生图完全一样。现在以图生图模式下指令会带上 `【生成方式】以图生图`、`【参考图】reference_image="…" denoise=…`、`【参考图尺寸】`，并要求「调用 image_gen_generate 时必须带 reference_image，漏掉就变成文生图了」。按钮文案也随模式变成「✨ 让 AI 以图生图」。
- **新增面板状态同步 + Agent 工具 `image_gen_panel_context`**：面板把当前状态（生成方式 / 模型 / LoRA / 参考图 / denoise / 尺寸 / 分类）POST 到 `/panel/context`，Agent 可以在用户说「按面板里的设置来 / 参考图我已经选好了」时读出来，直接把 `reference_image` 传给生图工具。
- **上传的参考图会本地留一份**（`data/ref_uploads/`），返回 `local_path`，这样非图库来源的参考图也能用绝对路径给 AI 引用。
- 前端入口升级到 `ui/index.1.1.4.js`。

## v1.1.3

- **修复 AI 一句话以图生图（Agent 工具不带参考图）**：`image_gen_generate` 新增 `reference_image`（图库图片 ID 或本地绝对路径）与 `denoise` 参数；传了参考图且宽高保持默认时会自动沿用参考图尺寸。实测 `reference_image="188"` + `denoise=0.55` 直接出图成功。
- **新增 Agent 工具 `image_gen_list_images`**：列出图库图片（ID / 分类 / 模型 / 尺寸 / 提示词片段），让 AI 在「用某张图做参考 / 复刻某张图」时能自己找到图片 ID，而不是编造路径。同步更新了 skill 文档与工具描述。
- **提示词输入框太小**：正向提示词从 3 行改为「最少 7 行、随内容自动增高到 26 行 + 可手动拖拽调整高度」，负向提示词最少 3 行、最多 12 行，两者都加了字数统计。长提示词现在改起来不用挤在小框里了。
- 前端入口升级到 `ui/index.1.1.3.js`。

## v1.1.2

- **新增以图生图（img2img）**：生图页顶部新增「0. 生成方式」= 文生图 / 以图生图。
  - 参考图来源：图库任意图片（点「从图库选」后点图即选中，或图片详情里点「用作参考图」）、本地上传。
  - 可调「重绘幅度 denoise」（0.4 轻微改动 / 0.6 常用 / 0.8 大改）与「尺寸跟随参考图」开关。
  - 后端 `build_img2img_workflow`（LoadImage → ImageScale → VAEEncode → KSampler(denoise) → VAEDecode）复用同一套模型 / CLIP / VAE / LoRA 解析，所有模型类型都支持。
  - 新接口 `POST /img2img/from-gallery/{id}`、`POST /img2img/upload`；`/generate/async` 增加 `generation_mode` / `source_image`。
- **修复分块高清修复在块里「长出新角色」**：分块模式会把提示词在每个分块上各执行一遍，原图提示词里的 `1girl / 角色名` 会让天空、衣褶这类空背景块凭空生成主体。现在分块模式默认改用画质词提示词（`UPSCALE_QUALITY_PROMPT`），denoise 默认 0.35→0.2，分块重叠 32→64、mask_blur 8→16、接缝修复 denoise 1.0→0.5；UI 新增「放大提示词」三选（自动 / 只用画质词 / 沿用原图）。
  - 实测（同一张图、同一 seed，与纯 ESRGAN 2× 对比）：旧参数平均偏差 12.27、最差分块相关性 0.856、20 块中 8 块相关性 <0.9；新参数平均偏差 7.78、最低相关性 0.921、**0 块 <0.9**。
- **放大末尾增加轻量锐化**：VAE 往返会让线稿变软，tiled/hires 末尾新增 `ImageSharpen`（默认 alpha 0.08，纯卷积不会新增内容）。实测清晰度指标 846 → 1790（纯 ESRGAN 基线 1567），且分块相关性仍保持 0 块 <0.9。
- **新增工作流自检**：面板「工作流切换」右上角「自检」+ `POST /workflows/self-check`。不跑图，直接拿绑定生成的工作流与 ComfyUI `object_info` 对账：节点是否存在、必填参数是否齐全、模型 / CLIP / VAE / LoRA 文件名是否在可用列表里。
- **「自动适配」按钮常驻**：之前只有当前模型未绑定工作流时才显示，导致已绑定的用户找不到入口。现在「工作流切换」卡片右上角常驻，选中模型时按该模型类型重建/修复绑定，未选模型时才走全量一键适配。
- **修复排队被误判超时**：ComfyUI 在跑其他任务时，插件提交的任务会一直 pending，原 300 秒硬超时直接报「生图超时」。现在排队时间不计入执行超时（排队上限 30 分钟），并把 `/object_info` 等接口超时从 5 秒提到 20 秒。
- 前端入口升级到 `ui/index.1.1.2.js`。

## v1.1.1

- **修复「开始分块高清修复」点了没反应**：`@router.post("/upscale/queue/start")` 装饰器被误挂在内部的 `_opts_from_query` 上，真正的 `api_upscale_queue_start` 没有注册路由。前端 POST 拿到 200（返回的是解析后的参数 JSON），但任务从未创建、待放区也不清空，所以既没有进度也没有报错。现已把装饰器挂回正确的处理函数，并核对了全部 58 个路由无同类错位。
- **前端不再静默失败**：`startUpscaleQueue` 现在校验响应里的 `task_id`，缺失时明确提示「后端未返回任务号，请完全退出并重启 QwenPaw Desktop」，并自动刷新待放区；按钮增加提交中状态（loading + 禁用 + 「提交中…」），空队列/未选模型给出行内提示。
- **放大三模式**：`fast`（纯 RealESRGAN 放大，最快）、`hires`（放大 + 低 denoise 高清修复，复用原图的模型/提示词/LoRA/VAE）、`tiled`（分块高清修复，512 分块，显存友好）。三种模式共用一套参数（倍数 / denoise / steps / cfg / 采样器 / 调度器 / 种子模式 / 分块大小 / 接缝修复）。
- **放大参数改为 options JSON 透传**，同时保留 `profile` / `category` 旧查询参数兼容；按模型类型自动挑选默认放大模型（二次元/插画 → anime_6B，写实 → x4plus）。
- 前端入口文件改为 `ui/index.1.1.1.js`，绕开桌面端 WebView 缓存。

## v1.1.0

- **新增 Z-Image-Turbo（Lumina2）工作流支持**：`z-image-turbo-fp8-e4m3fn.safetensors` 之前被误判成普通 checkpoint，导致 ComfyUI 返回 400（ckpt_name 不在列表）。现按真实结构构建独立工作流：
  - `UNETLoader`（FP8，`weight_dtype=fp8_e4m3fn`）加载 DiT。
  - `CLIPLoaderGGUF`（`type=lumina2`）加载 GGUF 文本编码器 `Qwen_3_4b-imatrix-IQ4_XS.gguf`。
  - `VAELoader` 加载 `ae.safetensors`。
  - `CLIPTextEncodeLumina2`（system_prompt=superior）+ `ConditioningZeroOut`（负向占位）。
  - `EmptySD3LatentImage` + `ModelSamplingAuraFlow(shift=3)` + `KSampler`。
- **修复 GGUF 文本编码器扫描缺失**：ComfyUI 的 `/api/models/text_encoders` 不返回 `.gguf`，导致 Lumina2 编码器扫不到。现从 `CLIPLoaderGGUF` 等节点的 `clip_name` 真实选项补扫 GGUF。
- **完善模型分类**：`classify_model` 新增 `z-image`/`z_image`/`zimage` 识别，归为独立 `z_image` 类型，不再落入 checkpoint 分支。
- **ComfyUI 错误透传**：`_run_comfyui` 之前把 400 的具体报错（哪个节点、哪个参数不对）吞掉，只显示笼统的「400 Bad Request」。现解析 `node_errors` / `error`，直接告诉用户是哪张图卡在哪个节点。

## v1.0.9

- **图库分页与排序/筛选统一**：排序与筛选下放到后端 SQL，分页加载与「最新优先」等排序在全库范围内生效，不再只对已加载的 40 张排序。
  - 后端排序统一使用 `generated_at`（真实生成时间），与 v1.0.4 的时间修复一致。
  - 新增 `/gallery/filters` 接口：模型 / LoRA 筛选下拉改为按当前分类全库统计，不再依赖当前页。
  - 切换排序、星级、模型、LoRA 筛选时自动重新加载图库。
- **生成时间人性化显示**：图库详情「生成时间」显示为「刚刚 / N 分钟前 / N 小时前 / N 天前」，超过 7 天显示具体日期时间。
- **修复本地上传放大不可用**：`doUpscaleUpload` 中错误的 `useState` 重置导致上传始终提示「先选择一张图片」，已改为直接传文件参数，上传后自动刷新待放区。
- **清理失效记录增强**：清理后自动刷新图库与筛选选项，并显示实际清理数量。
- **版本自检增强**：版本不一致提示新增「重新加载」按钮，用户无需重启即可重试。

## v1.0.6

- **恢复 AI 协作自定义工作流**：新增两个 agent 工具，AI 可以创建和注册自定义 ComfyUI 工作流。
  - `image_gen_register_workflow_preset` — AI 将自定义工作流 JSON 注册为预设
  - `image_gen_apply_workflow_preset` — 将工作流预设绑定到指定模型
  - AI 可根据 ComfyUI 可用节点构造 workflow JSON，用户无需手动配置
  - Skill 文档已更新，指导 AI 如何构建标准 ComfyUI API 工作流

## v1.0.5

- **放大功能重做**：新增「待放大区」，图片先加入队列再统一点击放大。
  - 图库选中图片后点「加入待放区」批量添加
  - 本地上传直接进入待放区
  - 待放区支持移除单张和清空全部
  - 选择放大模型后统一「开始放大」

## v1.0.4

- **修复图库图片时间信息错误**：图片的"生成时间"之前显示的是导入/扫描时间（`created_at`），现改为显示实际文件生成时间（`generated_at`）。
  - 输出目录扫描：从文件系统取 `st_mtime` 作为生成时间。
  - ComfyUI 历史记录扫描：从 `completed` 时间戳获取生成时间。
  - 实时生图/放大：用完成时的当前时间作为生成时间。
  - 排序和前端显示优先使用 `generated_at`，全量排序统一使用实际生成时间。
- `add_image()` 新增 `generated_at` 参数，旧记录不受影响，重新扫描可补齐。

## v1.0.3

- 修复「一键自动适配」点击后 404 的问题：后端新增 `POST /workflows/one-click-setup` 路由，无需传模型名即可全自动扫描并绑定最优主模型。
- 修复「强制修复」点击后 404 的问题：后端新增 `POST /repair` 路由，安全清理失效绑定和输出目录配置，不删除图库原图。
- 修复图库批量模式下「加入放大队列」按钮点击报错的问题：前端补上 `batchUpscale()` 函数，选中图片后自动切换到放大面板。
- 修复图库历史记录扫描去重逻辑失效：去重 key 与文件名交叉校验，避免重复下载同一张历史图片。
- 输出目录配置增加安全校验：禁止空路径、相对路径和系统目录，避免误操作。
- 强制修复界面文案修正：明确说明不会删除图库原图和图片记录。

## v0.9.3

- 修复图库切换分类时列表仍显示旧图片、响应卡顿的问题。
- 图库列表与分类读取不再隐式扫描整个 ComfyUI output；仅点击「扫描 ComfyUI」时执行全盘扫描。
- 切换分类时立即清空旧列表并显示加载状态；用请求序号防止旧请求的迟到响应覆盖新分类结果。

## v0.9.2

- 修复 ComfyUI PNG 元数据扫描只显示正向提示词的问题。
- 按 KSampler 的 `positive` / `negative` 连线准确提取正向与反向提示词；重新扫描可补齐旧图片记录。

## v0.9.1

- 图库详情新增所属分类选择器，可将已有图片实际移动到 ComfyUI output 内对应分类文件夹。
- 数据库同步更新图片分类、文件路径与文件名；目标重名时自动避免覆盖。
- 新增「全部分类」视图。


## v0.8.0

- 新增调试追踪功能：后端记录最后一次生成的完整提示词，通过 `/debug/last-generation` 端点可查看
- 新增前端调试面板：在「工作流参数」下方显示最后一次发送的提示词，方便排查提示词问题
- 新增 ComfyUI 画布同步说明：明确告知用户插件通过 API 提交工作流，不会改变 ComfyUI 画布显示
- 后端添加详细日志：记录发送到 ComfyUI 的提示词和反向提示词，便于调试
- 所有改动同时更新到 package 目录，确保打包版本与源码一致

## v0.5.0

- 架构重构：新增 `backend/comfy_adapter.py`，插件后端负责确定性 ComfyUI 能力发现、模型分类、工作流构建与绑定校验，AI 降级为提示词和解释辅助。
- `/image-gen/status` 改为完整资源扫描：返回 checkpoints、loras、vae、clip、text_encoders、unet、diffusion_models、controlnet、upscale_models，不再只截断前 50 个 checkpoint。
- 采样器和调度器改从 ComfyUI `/object_info` 中读取真实节点选项，而不是仅依赖手写枚举。
- 新增 Anima / Qwen UNet 工作流构建器：`UNETLoader + CLIPLoader(type=qwen_image) + VAELoader + KSampler`，默认 CFG 1.0、Euler/normal，LoRA 使用 `LoraLoaderModelOnly`。
- 后端 `/workflows/bind` 新增校验：Anima/Flux/Nunchaku/GGUF 等非 checkpoint 模型不能被错误保存成 Illustrious/SDXL 工作流。
- 新增 `/workflows/auto-bind`：用户选择模型后可一键自动适配工作流，减少“复制提示词让 AI 猜”的出错路径。
- 前端入口升级到 `ui/index.0.5.0.js`，无工作流状态下新增“一键自动适配”按钮。
- 新增 `docs-v0.5.0-architecture.md`，记录本次参考 SD Manager 后的职责边界和后续重构方向。

## v0.4.1

- 修复 QwenPaw Desktop / Electron WebView 可能继续加载旧版前端缓存的问题。
- 前端入口改为带版本号的 `ui/index.0.4.1.js`，升级后资源 URL 会变化，避免命中旧缓存。
- 后端新增 `/image-gen/version` 接口，用于前后端版本自检。
- 后端所有插件 API 响应加入 `Cache-Control: no-store` 等禁缓存响应头。
- 前端标题显示版本号，检测到前后端版本不一致时给出明确重启/清缓存提示。
- README 新增桌面端升级与缓存说明。


## v0.4.0

- 新增默认工作流预设：SDXL 通用、Illustrious / WAI、Pony、SD1.5、快速预览。
- 新增右侧面板工作流切换：可直接选择默认或自定义工作流，让插件开箱即可尝试生成。
- 新增“保存当前工作流”：保存当前面板参数为可复用预设，同名自动覆盖，避免重复堆积。
- 新增与 QwenPaw 产物库的本地联动：生成图片可登记为本地创作资产，便于检索、评分、备注与复用。
- 社区文案统一使用正式名称 ComfyUI，移除口语化缩写。
- 移除会修改宿主主聊天输入框或压缩宿主布局的高风险逻辑，降低 Desktop 渲染进程闪退风险。
- 补齐 requirements.txt 与 build_package.py，打包时只包含可审查运行文件，不包含 data、数据库、图片缓存或本地私有路径。

## v0.3.1-hotfix.2

- 修复社区文案中 ComfyUI 名称不正式的问题。
- 公开接口统一改为正式命名。
- 补齐基础依赖说明。

## v0.3.1-hotfix.1

- 移除写入主聊天输入框、修改宿主布局等高风险 DOM 操作。
- 改为复制提示词草稿到剪贴板，避免与 QwenPaw Desktop 前端状态冲突。
- 重写默认最小 ComfyUI 工作流提示词。

## 隐私与去个人化要求

发布前必须确认：

1. 文档、截图、默认数据不出现私人名称或真实项目名。
2. 不包含真实聊天记录、真实图片素材、真实图库数据库。
3. 不包含 API Key、账号信息或本机绝对路径。
4. 模型只显示文件名或脱敏名称，不展示本机完整路径。
5. 打包前清空 data、图库、数据库和缓存。
